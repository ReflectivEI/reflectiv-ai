{
  "name": "my-chat-agent-v2",
  "version": "10.2.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --outfile=worker.js --platform=node --target=esnext --external:cloudflare:*",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test src/**/*.test.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "esbuild": "^0.19.11",
    "typescript": "^5.3.3",
    "wrangler": "^3.22.1"
  }
}
